# Copy this file to `.env.local` and add your key to enable the real model:
#   cp .env.example .env.local
#
# Without a key, the app automatically falls back to the mock model, so it still runs.
# Get a key from OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY=
